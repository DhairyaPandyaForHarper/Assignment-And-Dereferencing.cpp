#include <iostream>
using namespace std;

int main() {
    int a, b;

    cout << "Enter two integers: ";
    cin >> a >> b;

    int* ptr_a = &a;
    int* ptr_b = &b;

    cout << "Value of a: " << *ptr_a << endl;
    cout << "Value of b: " << *ptr_b << endl;

    return 0;
}
